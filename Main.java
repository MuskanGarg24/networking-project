import src.controller.PeerController;

public class Main {
    public static void main(String[] args) {
        PeerController peerController = new PeerController();
        peerController.startApplication();
    }
}